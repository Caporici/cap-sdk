/**
 * @brief Initializes LVGL library, display drivers, and input devices.
 * * This function sets up the framebuffer flush callback and 
 * configures the evdev touch input with calibration.
 */
void ui_init(void);

